# URL de base de l'API.
# En developpement, "/api" passe par le proxy Vite (voir vite.config.ts).
# En production, renseigner l'URL complete du backend (ex: https://api.exemple.com).
VITE_API_URL=/api
